Media-status (watched-percent progress bar) port  -  fork 3.5.31 -> 3.5.32
Ported from upstream master (Arctic: Zephyr - Rounded v2.3.00)

WHAT WAS MISSING
  The fork rendered only a PVR resume bar (ListItem.Progress) inline in
  Square/Thumb/Fanart/Landscape widgets. Poster widgets (the "Continue
  Watching" episode row) had NO resume bar at all, and nothing used
  ListItem.PercentPlayed / WatchedEpisodePercent - so movie/episode/tvshow
  resume progress never showed on home widget tiles.

WHAT WAS ADDED (all additive - no existing behaviour changed)
  Includes_Expressions.xml
    + ShowProgressBarPercentPlayed
    + ShowProgressBarPercentTvShowsPlayed
  Includes_Colors.xml
    + Color_Widget_Progress_Bar_Background   (8FBBBBBB)
  Includes_Furniture.xml
    + Layout_Poster_Progress_Bar_PercentPlayed   (movies/videos/episodes via
      ListItem.PercentPlayed; tvshows via WatchedEpisodePercent)
    + Layout_Poster_Progress_Bar_PVR             (reusable PVR bar)
  Includes_Widgets.xml
    Wired the two includes into Def_Widget_Poster / _Square / _Thumb /
    _Fanart / _Landscape, using the same bottom/small/middle offsets as
    upstream. Existing inline PVR bars were left intact.

DELIBERATELY NOT PORTED
  Layout_Poster_Indicator_Status (upstream's unified watched-flag/banner).
  The fork already reimplements this as split _Icon / _Banner includes with
  Arabic-oriented variables and different texture paths (misc/... vs
  dialog/misc/...). Porting upstream's version would break the fork's
  working status flags. The two systems are independent.

ARABIC
  No new user-facing (#3xxxx) strings are introduced by this feature, so no
  translation work was required. ar_sa is already 100% complete vs en_gb
  (0 untranslated entries, verified).

ASSETS
  progress/progress-nib.png is already inside Textures.xbt - no asset work.

-----------------------------------------------------------------
3.5.33 FIX - Media status settings rows were not selectable
-----------------------------------------------------------------
Symptom: In Skin Settings > Furniture > Media status, the sub-rows
(Is playing / In progress / Unwatched / Watched / New / etc.) took
focus (highlighted) but pressing OK did nothing.

Cause: all ten Media-status radiobuttons (ids 9599, 9586, 95861,
95862, 9587, 95882, 9588, 95881, 9589) were missing an explicit
<width>. Every other row in the Furniture settings grouplist
declares <width>1278</width>; the grouplist uses
usecontrolcoords=true, so a row with no width gets a zero-width hit
area - it draws and highlights but has no clickable region, so OK
never reaches its <onclick>.

Fix: added <width>1278</width> to those ten rows, matching every
other row in the panel. No logic changed.

-----------------------------------------------------------------
3.5.34 FIX - Invisible toggle indicators (radio on/off etc.)
-----------------------------------------------------------------
Symptom: radiobutton on/off markers, sliders, scrollbars, indicator
and nowplaying glyphs, the color-picker box and volume/mute icons all
rendered blank across the skin (e.g. settings toggles had no visible
on/off state).

Cause: a fork-wide regression had stripped the "dialog/" prefix from
many texture paths, but those textures live under dialog/... inside
Textures.xbt. Kodi could not find e.g. buttons/radio-on.png (real
path dialog/buttons/radio-on.png), so it drew nothing.

Fix: restored the correct prefix on 44 texture paths (171 refs),
matching upstream master exactly:
  buttons/*        -> dialog/buttons/*
  buttons/mute.png    -> dialog/volume/mute.png
  buttons/volume.png  -> dialog/volume/volume.png
  addonstatus/*    -> dialog/addonstatus/*
  indicator/*      -> dialog/indicator/*
  kaitoast/*       -> dialog/kaitoast/*
  misc/*           -> dialog/misc/*   (incl. banner_status_overlay.png)
  nowplaying/*     -> dialog/nowplaying/*
  scrollbar/*      -> dialog/scrollbar/*
  slider/*         -> dialog/slider/*
Kodi built-in defaults and external resource-addon images were left
untouched.

-----------------------------------------------------------------
3.5.35 FIX - Media status marker glyph (clock / check / new) blank
-----------------------------------------------------------------
Symptom: after 3.5.34 the corner status marker drew only a solid dot
- the glyph inside it (in-progress clock, watched check, new, etc.)
was missing.

Cause: Layout_Poster_Indicator_Status_Icon and
Def_Media_Status_Banner_Symbol render the status glyph with
<font>SymbolPoster</font> (and the disc uses SymbolPosterBackground),
but neither font was defined in ANY fontset of the fork's Font.xml.
Kodi fell back to the default font, which lacks those private-use
codepoints (e.g. U+F484), so the glyph rendered blank. symbol.ttf
was already present and contains all needed glyphs; only the font
definitions were missing. (Pre-existing in 3.5.31 - not caused by
the media-status port.)

Fix: added SymbolPoster (size 20) and SymbolPosterBackground
(size 40), both -> symbol.ttf, to all three fontsets (Default,
ايفون, تونس), matching upstream master.

-----------------------------------------------------------------
3.5.36 FEATURE - AF3-style circular progress marker (optional)
-----------------------------------------------------------------
Ported the circular progress-ring media-status marker from
Arctic Fuse 3 (v5.9.10.5) as an OPTIONAL alternative to the
existing font-glyph marker. The glyph system is untouched and
remains the default.

What it looks like: a dark backing disc with a green ring that
fills clockwise by resume percent (ListItem.PercentPlayed for
movies/videos/episodes, WatchedEpisodePercent for tvshows), and
a full green ring + checkmark for watched items.

Added:
  media/progress/circle/p0.png .. p100.png   (101 loose frames, from AF3)
  media/indicator/checkmark.png, circle-diffuse.png,
    circle-colordiffuse.png                  (from AF3)
  Includes_Colors.xml    : ColorWatchedProgress (default ff03b585,
                           user-overridable via watchedprogresscolor.name)
  Includes_Furniture.xml : Defs_PercentPlayed_AF3 variable +
                           Layout_Poster_Circle_Progress include
  Widgets + all poster views: each existing
    Layout_Poster_Indicator_Status_Icon call now also offers
    Layout_Poster_Circle_Progress, switched by skin setting
    furniture.markers.af3 (glyph when off, circle when on).
    Banner mode is unaffected.
  SkinSettings.xml       : new toggle (id 95901) under
                           Furniture > Media status:
                           "Circle progress marker"

NOT ported: AF3's Object_Control / script.skinvariables framework
and the extra AF3 states (timers/library/collection). The port is
self-contained and needs no external script.

Enable: Settings > Skin Settings > Furniture > Media status >
        Circle progress marker.

-----------------------------------------------------------------
3.5.37 - AF3 circle marker: percentage number + episode count
-----------------------------------------------------------------
Extended Layout_Poster_Circle_Progress (the furniture.markers.af3
marker) with two centered overlays, font WatchedIconSmall:

  - IN PROGRESS: the resume percentage as a number inside the ring
      movies/videos/episodes -> ListItem.PercentPlayed %
      tvshows               -> WatchedEpisodePercent %  (only when
                               there are no unwatched episodes)
  - UNWATCHED EPISODES: the UnWatchedEpisodes count centered in the
      disc for tvshow/season (takes priority over the progress ring,
      matching AF3). Hidden when hide.markers.newcontent is set.

Priority per item: unwatched-episode count > progress ring/number >
watched check. Only affects the AF3 marker; glyph and banner modes
unchanged.

-----------------------------------------------------------------
3.5.38 - AF3 circle marker: larger, bolder number
-----------------------------------------------------------------
The percentage/episode-count number inside the AF3 circle marker
was too small (font WatchedIconSmall, size 20). Added a dedicated
font CircleMarkerNumber (RobotoNotoCJKsc-Bold, size 26) to all
three fontsets and pointed the three marker labels at it. Size 26
bold fills the 48px disc cleanly without touching the ring.

-----------------------------------------------------------------
3.5.39 - AF3 circle marker: 2x larger
-----------------------------------------------------------------
Doubled the AF3 circle marker: Layout_Poster_Circle_Progress
default size param 48 -> 96 (all width/height derive from it, so
every ring, disc, number and checkmark scale together). Number
font CircleMarkerNumber scaled 26 -> 48 in all three fontsets to
keep the percentage/count proportional in the larger disc.
